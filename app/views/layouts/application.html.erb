<!DOCTYPE html>
<html>
<head>
  <title>MjgServer</title>
  <%= favicon_link_tag 'favicon.ico' %>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <script type="text/javascript"
  src="http://cdn.mathjax.org/mathjax/latest/MathJax.js?config=TeX-AMS-MML_HTMLorMML">
  </script>
</head>
<body>

<% if not params[:controller].include? 'devise'%>
<!-- We are in a legitimate user account So...
     Let's display the menu-bar -->
  <table>
    <tr>
      <% if current_user.is_admin? %>
      <td><%= link_to "Manage Users", manage_users_list_path %></td>
      <% end %>
      <td><%= link_to "Todos", todos_path %></td>
      <td><%= link_to "Lyric Thief", lyric_thief_index_path %></td>
      <td><%= link_to "Tex Trier", tex_trier_index_path %></td>
      <td><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></td>
    </tr>
  </table>
<% end %>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>
